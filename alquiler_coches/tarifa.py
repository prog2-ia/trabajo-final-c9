#precio base, precio km, penalizacion_retraso 
#mostrar_info()


'''

class Tarifa:

    def __init__(self, precio_base: float, precio_km: float, penalizacion_retraso: float):
        self._precio_base = precio_base
        self._precio_km = precio_km
        self._penalizacion_retraso = penalizacion_retraso

    def calcular_precio(self, dias: int, kilometros: int) -> float:
        return (self._precio_base * dias) + (self._precio_km * kilometros)
'''